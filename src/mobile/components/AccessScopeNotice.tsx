import React from 'react';
import { StyleProp, ViewStyle } from 'react-native';

interface AccessScopeNoticeProps {
  title: string;
  message: string;
  style?: StyleProp<ViewStyle>;
}

export default function AccessScopeNotice(_: AccessScopeNoticeProps) {
  return null;
}
